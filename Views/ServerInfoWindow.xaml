<Window x:Class="RobloxVault.Views.ServerInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server Info" Width="320" SizeToContent="Height"
        WindowStartupLocation="Manual" Left="20" Top="20"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    <Border Background="#0D0D0F" CornerRadius="12"
            BorderBrush="#1E1E28" BorderThickness="1"
            MouseDown="Border_MouseDown">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" Opacity="0.75" ShadowDepth="0"/>
        </Border.Effect>
        <StackPanel Margin="20,16,20,20">

            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="8" Height="8" CornerRadius="4"
                            Background="{DynamicResource AccentBrush}"
                            Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Server Info"
                               Foreground="#E0E0EE" FontSize="13" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="✕" HorizontalAlignment="Right"
                        Style="{StaticResource IconButton}"
                        Foreground="#55557A" FontSize="11"
                        Click="CloseButton_Click"/>
            </Grid>

            <Border Background="#1A1A26" Height="1" Margin="0,0,0,16"/>

            <!-- Account name -->
            <TextBlock x:Name="AccountNameText"
                       Foreground="#55557A" FontSize="11"
                       Margin="0,0,0,14" TextWrapping="Wrap"/>

            <!-- Rows -->
            <StackPanel x:Name="InfoRows" Margin="0,0,0,0"/>

            <!-- Last updated -->
            <TextBlock x:Name="LastUpdatedText"
                       Foreground="#303044" FontSize="10"
                       Margin="0,14,0,0" HorizontalAlignment="Right"/>
        </StackPanel>
    </Border>
</Window>